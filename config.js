/**
 * Public browser configuration for SPINE.
 *
 * The Facebook App ID is safe to expose. Never put a Facebook App Secret here.
 * After changing this file, upload it with the rest of the static site.
 */
export const SPINE_CONFIG = Object.freeze({
  facebookAppId: "",
  facebookApiVersion: "v25.0",
});
